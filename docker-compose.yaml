services:
  traefik:
    image: traefik:v3
    container_name: traefik

    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--api.insecure=true"

    ports:
      - "80:80"
      - "8080:8080"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

    networks:
      - omniai-network

    restart: unless-stopped

  speech:
    build:
      context: ./omniai/services/speech
      dockerfile: Dockerfile

    expose:
      - "8000"

    environment:
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
      OMNIAI_HOME: /root/.omniai/models

    labels:
      - "traefik.enable=true"

      # Route every /v1/audio request to OmniTTS
      - "traefik.http.routers.speech.rule=PathPrefix(`/v1/audio`)"
      - "traefik.http.routers.speech.entrypoints=web"
      - "traefik.http.routers.speech.service=speech"

      # Internal container port
      - "traefik.http.services.speech.loadbalancer.server.port=8000"

    volumes:
      - /home/moussa/coding/omniai-dev/OmniAI/models:/root/.omniai/models

    networks:
      - omniai-network

    restart: unless-stopped

networks:
  omniai-network:
    name: omniai-network