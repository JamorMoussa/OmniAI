[project]
name = "omnitts-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "omnitts",
]

[tool.uv.sources]
omnitts = { git = "https://github.com/JamorMoussa/OmniTTS.git" }
